# ── Docker Compose Local Development ─────────────────
# Copy this file to .env and fill in values:
#   cp .env.example .env
#
# These are ONLY used by docker-compose.yml for local dev.
# They are NOT production credentials.

POSTGRES_USER=machi
POSTGRES_PASSWORD=machi_dev
POSTGRES_DB=machi
DOCKER_SECRET_KEY=docker-dev-secret-not-for-production
